services:
  app:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=5173
    command: sh -c "npm install && npm run dev"
    ports:
      - "5173:5173"
